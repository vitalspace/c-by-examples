#include <stdio.h>

int main()
{

    // Declare an array of integers of size 3
    int myArray[3];

    // Assign values to some array elements
    myArray[0] = 1;
    myArray[1] = 2;
    myArray[2] = 3;

    // Print the value of the second element 
    printf("The value %d\n", myArray[1]);
    
    // Return 0 to indicate that the program finished without errors
    return 0;
}