#include <stdio.h>

int main () {

    // Declare and assign an array
    int myArray [3] = {1, 2, 3};

    // Print the index 0;
    printf("%d", myArray[0]);

    // Return 0 to indicate that the program finished without errors
    return 0;
}